# Dashboard

Save the published dashboard screenshot here as `dashboard.png` and link it from the root README.

To capture: open the dashboard in Tableau Public Desktop, hide the Show Me panel, then
Worksheet > Export > Image, or use Cmd+Shift+4 on macOS for a region capture.

Live version: add the Tableau Public URL to the root README once published.
